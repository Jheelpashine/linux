echo "enter the number"
read number
rev=0
while [ $number -gt 0 ]
do
    digit=$(( numbe % 10 ))
    rev=$(( rev * 10 + digit ))
    number=$(( number / 10 ))
done
echo "reverse of the number is: $rev"
